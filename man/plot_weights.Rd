% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_weights.R
\name{plot_weights}
\alias{plot_weights}
\alias{plot_weights.bscmfit}
\alias{plot_weights.bscm_ldo}
\title{Visualize donor weights}
\usage{
plot_weights(x, ...)

\method{plot_weights}{bscmfit}(
  x,
  point_estimate = "median",
  order = NULL,
  coverage = c(0.5, 0.95),
  linewidth = 1,
  point_size = 2,
  reverse = NULL,
  ...
)

\method{plot_weights}{bscm_ldo}(
  x,
  type = "weight",
  point_estimate = "median",
  coverage = 0.95,
  linewidth = 1,
  point_size = 2,
  reverse = TRUE,
  ...
)
}
\arguments{
\item{x}{[\code{bscmfit} or \code{bscm_ldo}]\cr Output from \code{\link[=bscm]{bscm()}} or
\code{\link[=leave_donor_out]{leave_donor_out()}}.}

\item{...}{Ignored.}

\item{point_estimate}{[´character(1)\verb{] Should the point estimate in weight plot correspond to posterior }"median"\verb{(the default), or}"mean"`?}

\item{order}{[\code{character()} or \code{NULL}]\cr Order of donors in y-axis. Either
a vector of donor names, or \code{"descending"} / \code{"ascending"} to
rank donors by posterior means of donor weights. Default is \code{"descending"}.
Use \code{NULL} to use the original donor order. Ignored for \code{bscm_ldo} method
which uses the ordering which was used in \code{\link[=leave_donor_out]{leave_donor_out()}}.}

\item{coverage}{[\code{numeric()}]\cr Coverages of posterior intervals of donor
weights when \code{type = "weight"}. By default 50\% and 95\% posterior
intervals are drawn.}

\item{linewidth}{[\code{numeric(1)}]\cr Maximum line width used for intervals or
donor rank trajectories. Default is \code{1}.}

\item{point_size}{[\code{numeric(1)}]\cr Point size for point estimates or
trajectory endpoints. Default is \code{2}.}

\item{reverse}{[\code{logical(1)}]\cr Should the y-axis be reversed? Default is
\code{NULL}, in which case y-axis is reversed except when
\code{order  = "ascending"}, so in both ascending and descending ordering of
donors leads to plot where donor with largest weight is on top.}

\item{type}{[\code{character(1)}]\cr For \code{bscm_ldo} objects, plot posteriors
of donor weights (\code{"weight"}) or donor weight ranks (\code{"rank"}).
Default is \code{"weight"}, which is also only option for \code{bscmfit} objects.}
}
\value{
A \code{ggplot} object, or a named list of \code{ggplot} objects for models
with multiple treated units.
}
\description{
\code{plot_weights()} visualizes posterior summaries of donor weights for a
fitted Bayesian synthetic control model. When applied to the output of
\code{\link[=leave_donor_out]{leave_donor_out()}}, it instead visualizes how donor
weights or their ranks change across the leave-out runs.
}
