% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfo.R
\name{lfo}
\alias{lfo}
\alias{lfo.bscmfit}
\title{Leave-Future-Out Cross-Validation}
\usage{
lfo(x, ...)

\method{lfo}{bscmfit}(x, L, exact = FALSE, k_threshold = 0.7, ...)
}
\arguments{
\item{x}{[\code{bscmfit}]\cr The output returned by \code{\link[=bscm]{bscm()}}.}

\item{...}{Additional arguments passed on to \code{\link[=bscm]{bscm()}} when refitting.}

\item{L}{[\code{integer(1)}]\cr Minimum number of pre-treatment observations
used for the first fit. Must satisfy \verb{2 <= L <= T_pre_min - 2}, where
\code{T_pre_min = min(T_pre)}. Too small value of \code{L} can lead to unstable
estimation, so a value of at least \code{10} is recommended.}

\item{exact}{[\code{logical(1)}]\cr If \code{TRUE}, computes exact LFO by refitting
at every step. If \code{FALSE} (the default), uses the approximate PSIS-LFO
method.}

\item{k_threshold}{[\code{numeric(1)}]\cr Threshold for the Pareto \code{k} diagnostic
that triggers a model refit. Default is \code{0.7}. Ignored when \code{exact = TRUE}.}
}
\value{
An object of class \code{bscm_lfo}, a list with components:
\itemize{
\item \code{ELPD}: Total expected log predictive density.
\item \code{ELPD_SE}: A crude approximation of standard error of ELPD, ignoring
serial dependency of ELPD estimates.
\item \code{ELPDs}: Vector of per-step ELPDs (length \code{T_pre_min - L}). Element \code{k}
is the ELPD for predicting observations at \code{L + k}.
\item \code{ks}: Pareto \code{k} values (length \code{T_pre_min - L - 1}, \code{NULL} for exact
LFO).
\item \code{refits}: Time indices at which the model was re-estimated.
\item \code{L}: The value of \code{L} used.
\item \code{T_pre_min}: The minimum pre-treatment period length across treated
units.
\item \code{times}: Vector of all unique time values from the original data.
\item \code{time}: Name of the time variable.
\item \code{k_thres}: The Pareto k threshold used.
}
}
\description{
Estimates the leave-future-out (LFO) expected log predictive density (ELPD)
for \code{bscmfit} models. The LFO-CV is performed over the pre-treatment period:
at each step \code{t} (from \code{L} to \code{T_pre_min - 1}), the model is evaluated on
its ability to predict the next pre-treatment observation \code{y[t+1]} having
been fitted on \code{y[1:t]}. For models with multiple treated units, all units
must have at least \code{L + 1} pre-treatment observations, and only the first
\code{min(T_pre)} time points are used so the number of treated units stays
constant throughout the evaluation period. In this case, a joint ELPD of
all treated units is computed at each step.
}
\examples{
\donttest{
lfo_approx <- lfo(fit_single_treated, L = 10)
lfo_approx

lfo_exact <- lfo(fit_single_treated, L = 10, exact = TRUE)
lfo_exact
}
}
\references{
Bürkner PC, Gabry J, and Vehtari A (2020).
Approximate leave-future-out cross-validation for Bayesian time series
models. \emph{Journal of Statistical Computation and Simulation}, 90(14),
2499--2523, \url{doi:10.1080/00949655.2020.1783262}.
}
