% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R2.R
\name{loo_R2.bscmfit}
\alias{loo_R2.bscmfit}
\alias{loo_R2}
\alias{bayes_R2.bscmfit}
\alias{bayes_R2}
\title{Bayesian R-squared value}
\usage{
\method{loo_R2}{bscmfit}(object, summary = TRUE, probs = c(0.025, 0.975), fixed_seed = TRUE, ...)

\method{bayes_R2}{bscmfit}(object, summary = TRUE, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{object}{[\code{bscmfit}]\cr The model fit object.}

\item{summary}{[\code{logical(1)}]\cr If \code{TRUE} (the default), returns posterior
mean, standard deviation, posterior quantiles (as defined by the
\code{probs} argument), and MCMC convergence measures.
If \code{FALSE}, returns the posterior samples instead.}

\item{probs}{[\code{numeric()}]\cr Probabilities for quantile summaries.
Default is \code{c(0.025, 0.975)}.}

\item{fixed_seed}{[logical(1)]\cr If \code{TRUE} (the default), fixes the seed
of random number generator (RNG) so that \code{loo_R2}, which uses Bayesian
bootstrap, returns identical results in repeated calls for the same model
object. On exit, the state of the RNG is restored to the original state.}

\item{...}{Ignored.}
}
\value{
\code{data.frame} of posterior summary of (LOO-adjusted) R-squared values.
}
\description{
\code{bayes_R2} computes the Bayesian \eqn{R^2} measure of model fit for a
Bayesian synthetic control model, while \code{loo_R2} computes a leave-one-out
adjusted version of the same quantity.
}
\examples{
bayes_R2(fit_single_treated)
}
\references{
Gelman A, Goodrich B, Gabry J, and Vehtari A (2019). R-squared for
Bayesian regression models. \emph{The American Statistician}. 73(3), 307--309,
\url{doi:10.1080/00031305.2018.1549100}.
}
