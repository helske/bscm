% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_covariates.R
\name{plot_covariate_imbalance}
\alias{plot_covariate_imbalance}
\alias{plot_covariate_imbalance.bscmfit}
\title{Plot the covariate imbalance of a Bayesian synthetic control model}
\usage{
plot_covariate_imbalance(x, ...)

\method{plot_covariate_imbalance}{bscmfit}(
  x,
  unit = NULL,
  average = FALSE,
  probs = c(0.025, 0.975),
  alpha = 0.25,
  ...
)
}
\arguments{
\item{x}{[\code{bscmfit}]\cr The model fit object.}

\item{...}{Ignored.}

\item{unit}{[\code{character()}]\cr Treated units to plot. If \code{NULL} (the
default), all treated units are plotted.}

\item{average}{[\code{logical(1)}]\cr If \code{TRUE}, plots the imbalance averaged
over the treated units instead of unit-specific imbalances. The default
is \code{FALSE}.}

\item{probs}{[\code{numeric(2)}]\cr Vector of length two defining the limits of
the posterior interval. Default is \code{c(0.025, 0.975)}.}

\item{alpha}{[\code{numeric(1)}]\cr Opacity of the posterior interval ribbon.
Default is \code{0.25}.}
}
\value{
A \code{ggplot} object when a single treated unit is plotted, and a
named list of \code{ggplot} objects otherwise.
}
\description{
Plots the posterior mean and interval of the covariate imbalances over
time. See \code{\link[=covariate_imbalance]{covariate_imbalance()}} for the definition of the imbalance.
}
\examples{
plot_covariate_imbalance(fit_single_treated, probs = c(0.05, 0.95))
}
\seealso{
\code{\link[=covariate_imbalance]{covariate_imbalance()}}.
}
