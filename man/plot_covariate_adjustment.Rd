% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_covariates.R
\name{plot_covariate_adjustment}
\alias{plot_covariate_adjustment}
\alias{plot_covariate_adjustment.bscmfit}
\title{Plot the covariate adjustments of a Bayesian synthetic control model}
\usage{
plot_covariate_adjustment(x, ...)

\method{plot_covariate_adjustment}{bscmfit}(
  x,
  unit = NULL,
  average = FALSE,
  probs = c(0.025, 0.975),
  alpha = 0.5,
  scales = "free_y",
  ...
)
}
\arguments{
\item{x}{[\code{bscmfit}]\cr The model fit object.}

\item{...}{Optional arguments passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{unit}{[\code{character()}]\cr Treated units to plot. If \code{NULL} (the
default), all treated units are plotted.}

\item{average}{[\code{logical(1)}]\cr If \code{TRUE}, plots the adjustments averaged
over the treated units instead of unit-specific adjustments. The default
is \code{FALSE}.}

\item{probs}{[\code{numeric(2)}]\cr Vector of length two defining the limits of
the posterior interval. Default is \code{c(0.025, 0.975)}.}

\item{alpha}{[\code{numeric(1)}]\cr Opacity of the posterior interval ribbon.
Default is \code{0.5}.}

\item{scales}{[\code{character(1)}]\cr Should the panel scales be free
(\code{"free_y"}, the default) or fixed? Passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}
}
\value{
A \code{ggplot} object when a single treated unit is plotted, and a
named list of \code{ggplot} objects otherwise.
}
\description{
Plots the posterior mean and interval of the covariate adjustments over
time, with one panel per predictor. See \code{\link[=covariate_adjustment]{covariate_adjustment()}} for the
definition of the adjustment.
}
\examples{
plot_covariate_adjustment(fit_single_treated)
}
\seealso{
\code{\link[=covariate_adjustment]{covariate_adjustment()}}.
}
