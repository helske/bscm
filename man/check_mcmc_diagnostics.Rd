% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_mcmc_diagnostics.R
\name{check_mcmc_diagnostics}
\alias{check_mcmc_diagnostics}
\alias{check_mcmc_diagnostics.bscmfit}
\title{Check the validity of the posterior of \code{bscmfit} object}
\usage{
check_mcmc_diagnostics(x, ...)

\method{check_mcmc_diagnostics}{bscmfit}(x, check_all = FALSE, warn = TRUE, ...)
}
\arguments{
\item{x}{[\code{bscmfit}]\cr The model fit object.}

\item{...}{Ignored.}

\item{check_all}{[\code{logical(1)}]\cr If \code{FALSE} (the default),
variable-specific diagnostics are computed only for the model parameters and
the treatment effect estimates, but not for the other generated quantities
(e.g., RMSE estimates, pointwise log-likelihood values).
If \code{TRUE}, diagnostics are computed for all output variables. Note that you
get these  also with \code{\link[=summary.bscmfit]{summary.bscmfit()}}.}

\item{warn}{[\code{logical(1)}]\cr If \code{TRUE} (the default), generates and
(typically) prints out a warning in a case of problematic results. Setting
this to \code{FALSE} silently returns the check results.}
}
\value{
Invisibly returns a list containing the results of the check.
}
\description{
This function is automatically called at the end of \code{\link[=bscm]{bscm()}} to check that
the output can be trusted in terms of convergence of the MCMC sampling.
Checks consists of the common diagnostics of Hamiltonian Monte Carlo variant used by
Stan, as well as the Rhat values and effective sample sizes of all model
parameters and derived variables. See \code{\link[rstan:check_hmc_diagnostics]{rstan::check_hmc_diagnostics()}} and
\code{\link[posterior:draws_summary]{posterior::default_convergence_measures()}} for details on the definitions
of these.
}
\details{
Typical reasons for sampling issues can be a result of short pre-treatment
period and/or too many/few donors, units have very different series so that
the convex hull assumption is not even approximately plausible. Other
reasons include using priors which are incompatible with the data.
In addition, too large standard deviation \eqn{kappa} can
cause numerical issues (divergences) with Stan.
}
\references{
https://mc-stan.org/learn-stan/diagnostics-warnings.html
}
