% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_draws.R
\name{as_draws_rvars.bscmfit}
\alias{as_draws_rvars.bscmfit}
\alias{as_draws_rvars}
\title{Return posterior draws from BSCM fit as a draws_rvars object}
\usage{
\method{as_draws_rvars}{bscmfit}(x, parameters = NULL, inc_warmup = FALSE, include = TRUE, ...)
}
\arguments{
\item{x}{[\code{bscmfit}]\cr The model fit object.}

\item{parameters}{[\code{character()}]\cr Vector of parameter names. When
\code{NULL}, (the default), corresponds to a relevant subset of
\code{c("alpha", "beta", "sigma", "kappa", "rho")}. Other possible
choices are \code{"gamma"} (time-varying regression coefficients) and \verb{"lp__}
(log-posterior values without constants).}

\item{inc_warmup}{[\code{logical(1)}]\cr Whether to include warmup draws.
Default is \code{FALSE}.}

\item{include}{[\code{logical(1)}]\cr If \code{TRUE} (the default), output includes
only the variables defined by the argument \code{parameters}. If \code{FALSE}, these
variables are excluded from the output. If \code{NULL}, same as \code{TRUE} but
variables not present in the model object are silently ignored
(whereas \code{TRUE} throws an error).}

\item{...}{Ignored.}
}
\value{
An object of class \code{draws_rvars} containing the posterior draws of
the specified parameters.
}
\description{
Return posterior draws from BSCM fit as a draws_rvars object
}
\examples{
as_draws_rvars(fit_single_treated, parameters = c("alpha", "sigma"))
}
\seealso{
\code{\link[=as_draws.bscmfit]{as_draws.bscmfit()}} for the \code{draws_array} representation, and
\code{\link[posterior:draws_summary]{posterior::summarise_draws()}} for computing posterior summaries of the
draws.
}
