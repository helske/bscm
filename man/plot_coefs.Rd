% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_coefs.R
\name{plot_coefs}
\alias{plot_coefs}
\alias{plot_coefs.bscmfit}
\title{Visualize regression coefficients of Bayesian synthetic control model}
\usage{
plot_coefs(x, ...)

\method{plot_coefs}{bscmfit}(
  x,
  type = c("fixed", "varying"),
  probs = c(0.025, 0.975),
  combine = FALSE,
  alpha = 0.5,
  scales = "free_y",
  ...
)
}
\arguments{
\item{x}{[\code{bscmfit}]\cr Output from \code{\link[=bscm]{bscm()}}.}

\item{...}{Ignored.}

\item{type}{[\code{character(1)}]\cr Type of coefficients to plot. Either
\code{"fixed"} (the default) for time-constant coefficients or \code{"varying"} for
time-varying coefficients.}

\item{probs}{[\code{numeric(2)}]\cr Vector of length two defining the limits of
the posterior interval. Default is \code{c(0.025, 0.975)}.}

\item{combine}{[\code{logical(1)}]\cr If \code{TRUE} and \code{type = "varying"}, plot
the total effect (beta + gamma) instead of gamma alone.
Ignored when \code{type = "fixed"}. Default is \code{FALSE}.}

\item{alpha}{[\code{numeric(1)}]\cr Opacity of the credible-interval ribbon
(used only when \code{type = "varying"}). Default is \code{0.5}.}

\item{scales}{[\code{character(1)}]\cr Passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}
(used only when \code{type = "varying"}). Default is \code{"free_y"}.}
}
\value{
A \code{ggplot} object.

A \code{ggplot} object.
}
\description{
Plots posterior means and posterior intervals for the model coefficients.
For time-constant coefficients (\code{type = "fixed"}), a horizontal point-range
plot is produced. For time-varying coefficients (\code{type = "varying"}), a
ribbon-and-line plot over time is produced, faceted by parameter.
}
\examples{
plot_coefs(fit_single_treated)
}
